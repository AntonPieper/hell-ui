{
  "description": "foundation without CSS: root and /core entries plus behavior-only Part Style Map controls with package-wide light peers",
  "peerGroup": "core",
  "forbiddenDependencies": [
    "@tanstack/angular-table",
    "@tanstack/virtual-core",
    "@codemirror/view",
    "pdfjs-dist"
  ],
  "smoke": {
    "steps": [
      {
        "selector": "[data-test-id=\"root-core-status\"]",
        "textIncludes": "Root core contract: 1 result"
      }
    ]
  }
}
