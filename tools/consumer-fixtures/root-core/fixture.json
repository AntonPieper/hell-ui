{
  "description": "foundation without CSS: root, /core, and /testing entries plus behavior-only Part Style Map controls with package-wide light peers",
  "peerGroup": "core",
  "forbiddenDependencies": [
    "@tanstack/angular-table",
    "@tanstack/virtual-core",
    "@codemirror/view",
    "pdfjs-dist"
  ],
  "smoke": {
    "steps": [
      {
        "selector": "[data-test-id=\"root-core-status\"]",
        "textIncludes": "Root core contract: 1 result"
      },
      {
        "selector": "[data-test-id=\"testing-entry-status\"]",
        "textIncludes": "Testing entry contract: 9 harnesses"
      }
    ]
  }
}
