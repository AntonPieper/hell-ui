/**
 * Public seam for custom floating surfaces.
 *
 * Apply `[hellFloatingElement]` to portaled/custom floating content so it is treated as
 * inside the same Floating Interaction and participates in scoped dismissal checks.
 */
export { HellFloatingElement } from './floating-scope';
