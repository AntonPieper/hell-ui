kind: Breaking
body: >-
  `HellSpinner` moves from `hell-ui/skeleton` to its own `hell-ui/spinner`
  entry point, together with its labels and stylesheet. Markup and behavior
  are unchanged.
time: 2026-07-24T10:00:22.000Z
custom:
  migration: >-
    Import the spinner component and types from `hell-ui/spinner`, rename
    skeleton label overrides to the spinner token, and import
    `hell-ui/spinner/styles.css` wherever the skeleton stylesheet previously
    covered spinners.
